version: '3'
services:
  recserver:
    build:
      context: ../
      dockerfile: Dockerfile
    # image: rec-server
    # stdin_open: true # docker run -i
    # tty: true        # docker run -t
    ports:
      - "5000:5000"
    depends_on:
       - postgresdb
    # volumes:
    #   - .:/app
  postgresdb:
    build:
      context: ../test_db/
      dockerfile: Dockerfile    
    #image: "postgres-db"
    environment:
       POSTGRES_PASSWORD: postgres